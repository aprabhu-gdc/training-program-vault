[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -ra
markers =
    live: exercises real external services (Azure OpenAI + local LanceDB build). Skipped unless credentials are present.
    slow: slower tests (index builds, large inputs).
